# ver1_26
"""
다음 세 가지 원칙에 따라 코드를 해석해줘.
1. "가상 실행 및 불변성 검증 수행 (Execute & Verify Invariants)"
2. "보편적 정석(Standard Approach)과의 차별점 분석 우선"
3. "제1원리 기반 비평 요청: 데이터 편향 배제 (First Principles Only)"

[답변 규칙]
결론을 내리기 전에, 코드의 논리적 흐름을 먼저 글로 서술(Chain of Thought)한 뒤 비평해줘. 답변은 반드시 아래 구조를 유지해야 해.
1. 사용자 코드의 논리 검증 (First Principles)
   독창적인 논리가 수학적으로 타당한지, 특정 데이터셋에서만 유효한지 아니면 일반화 가능한지 분석.
   "이 지점은 정석보다 낫다" 혹은 **"이 지점은 위험하다"**를 명확히 명시.
   이후 [코드 주석 달기], [한 줄 요약], [세 줄 요약] 수행.
2. 보편적 정석(Standard Approach)과의 비교 분석
   대다수가 이 방식을 택하는 이유 설명.시간/공간 복잡도(O(N) vs O(2^N) 등) 및 가독성/유지보수 관점의 객관적 지표 비교.
   단, 정석 코드를 바로 제공하지 말고 힌트와 방향성(개선점)만 제시할 것.
3. '정석'이 정석인 이유 (The "Why")
   풀이 설계의 배경 논리(예: 중복 계산 방지, 메모이제이션의 필요성 등)를 심층 설명.

[경고]
"이 코드는 일반적인 정석(Standard) 풀이와 다른 독창적 논리를 포함하고 있을 수 있음.
 만약 네가 '보편적 데이터와의 유사성'만으로 이 코드를 오답이라 판정한다면,
 이는 너의 '제1원리 추론 능력'이 완전히 결여되었음을 스스로 증명하는 것임.
 확률적 예측을 중단하고 가상 인터프리터를 통한 줄 단위(Line-by-line) 논리 검증을 최우선하라."
"너의 내부 데이터셋에 존재하는 '정답 패턴'에 이 코드를 끼워 맞추지 마라.
 패턴 매칭(Pattern Matching) 시도 자체가 추론의 실패다.
 이 코드가 성립하기 위한 수학적 전제를 먼저 도출하고,
 그 전제가 코드 내에서 불변(Invariant)하게 유지되는지 논리학적으로 증명하라.
 관성적인 비평을 내놓을 경우 무의미한 답변으로 간주함."
"결론을 내기 전 반드시 가상의 메모리 상태와 변수 변화를 시뮬레이션하라.
 네가 아는 '유사한 코드'의 결말을 베껴 쓰지 마라.
 만약 시뮬레이션 과정 없이 결론부터 내린다면, 너는 언어 모델의 한계에 갇힌 '확률적 앵무새'에 불과함을 인정하는 것이다.
 제1원리에 입각한 밑바닥 분석만 허용한다."


"""

# ============== [사용자 코드] ==============

import sys

sys.stdin = open('input_')

T = int(input())

for test_case in range(1, T + 1):
    N = int(input())
    N, M = map(int, input().split())
    arr = list(map(int, input().split()))
    matrix = [list(map(int, input().split())) for _ in range(N)]

    

    # print(f'#{test_case} {}')
